{
  "name": "derawan-gallery-api",
  "version": "2.0.0",
  "description": "Dynamic gallery API for Derawan Archipelago Encyclopedia",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo 'No build step required for vanilla Node.js'",
    "sync": "node scripts/sync-gdrive-local.js",
    "generate": "node scripts/generate-gallery.js"
  },
  "dependencies": {
    "chokidar": "^3.6.0",
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.19.2",
    "googleapis": "^173.0.0",
    "node-cron": "^4.6.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
